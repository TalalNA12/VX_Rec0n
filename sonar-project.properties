sonar.projectKey=VX_Rec0n
sonar.projectName=VX_Rec0n
sonar.host.url=http://localhost:9000

sonar.sources=backend,frontend
sonar.sourceEncoding=UTF-8
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/.venv/**,**/venv/**,**/.git/**,**/__pycache__/**

# Python
sonar.python.version=3.12
# If you have tests:
# sonar.tests=backend/tests
# sonar.python.coverage.reportPaths=backend/coverage.xml

# Frontend (Vitest/Jest) coverage if you have it:
# sonar.javascript.lcov.reportPaths=frontend/coverage/lcov.info

# (Optional) make SCM explicit; still need commits for blame:
sonar.scm.provider=git
